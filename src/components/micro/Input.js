import React from 'react';

const Input = props => {
    return (
        <div>
            <label>Label</label>
            <PlainInput />
            <span>status</span>
        </div>
    );
};

export default Input;
